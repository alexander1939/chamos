<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crear cuenta</title>
    <!-- Incluir Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/auth/login.css') }}">
</head>

<body>

    <div class="container">
        <div class="form-container">
            <form id="registerForm">
                <h1>Crear cuenta</h1>

                <!-- Mensajes de error -->
                <div id="errorMessages" style="display:none;">
                    <div class="alert alert-danger" role="alert">
                        <span id="errorText"></span>
                    </div>
                </div>

                <!-- Mensajes de éxito -->
                <div id="successMessages" style="display:none;">
                    <div class="alert alert-success" role="alert">
                        <span id="successText"></span>
                    </div>
                </div>

                <!-- Campos del formulario -->
                <div class="form-group">
                    <input type="text" id="name" class="form-input" placeholder="Nombre" required>
                </div>

                <div class="form-group">
                    {{ register_form.phone(class_="form-input", placeholder="Teléfono") }}
                    {% for error in register_form.phone.errors %}
                    <div class="error">{{ error }}</div>
                    {% endfor %}
                </div>

                <div class="form-group">
                    {{ register_form.surnames(class_="form-input", placeholder="Apellidos") }}
                    {% for error in register_form.surnames.errors %}
                    <div class="error">{{ error }}</div>
                    {% endfor %}
                </div>

                <div class="form-group">
                    <input type="email" id="email" class="form-input" placeholder="Correo" required>
                </div>

                <div class="form-group">
                    <input type="password" id="password" class="form-input" placeholder="Contraseña" required>
                </div>

                <!-- Checkbox de aviso de privacidad -->
                <div class="form-group">
                    <label for="terms">
                        <input type="checkbox" id="terms" required> Acepto el
                        <a href="#">Aviso de Privacidad</a>
                    </label>
                    <div class="text-danger" id="termsError" style="display:none;">Debes aceptar los términos</div>
                </div>

                <!-- Botón de Crear cuenta -->
                <button type="submit" class="form-button" id="submitButton" disabled>Crear cuenta</button>

                <br>
                <p style="margin-top:20px">¿Tienes cuenta? <a href="#">Iniciar sesión</a></p>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script src="{{ url_for('static', filename='js/apis/register_api.js') }}"></script>


</body>

</html>